<Page x:Class="Student_Attendance_System.Views.TeacherDashboard"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Teacher Dashboard" Background="Transparent" Loaded="Page_Loaded">

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock x:Name="txtTitle" Text="先生用ポータル (Teacher Portal)" FontSize="28" FontWeight="Bold" Foreground="White"/>
            <TextBlock x:Name="txtCurrentDate" Text="Loading..." Foreground="#94A3B8" FontSize="14" Margin="0,5,0,0"/>
        </StackPanel>

        <TabControl Grid.Row="1" x:Name="MainTabControl" Background="Transparent" BorderThickness="0">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border x:Name="Border" Margin="0,0,10,0" Padding="15,8" Background="#0DFFFFFF" CornerRadius="10,10,0,0" BorderThickness="1,1,1,0" BorderBrush="#26FFFFFF">
                                    <ContentPresenter x:Name="ContentSite" ContentSource="Header" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="#1AFFFFFF"/>
                                        <Setter Property="TextBlock.Foreground" Value="#38BDF8"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="Foreground" Value="White"/>
                </Style>
            </TabControl.Resources>

            <TabItem x:Name="tabClass" Header=" 📅 本日の授業開始 ">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0,15,0,0">
                    <StackPanel x:Name="pnlDailyPeriods">
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <TabItem x:Name="tabManage" Header=" 📝 出席管理 ">
                <Border Background="#0DFFFFFF" Margin="0,15,0,0" Padding="25" CornerRadius="20" BorderThickness="1" BorderBrush="#26FFFFFF">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" x:Name="txtListTitle" Text="リアルタイム出席リスト" FontWeight="Bold" FontSize="20" Foreground="#38BDF8" Margin="0,0,0,15"/>

                        <DataGrid Grid.Row="1" x:Name="dgAttendance" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" 
                                  Background="Transparent" Foreground="White" BorderThickness="0" RowBackground="Transparent" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#1AFFFFFF">
                            <DataGrid.Resources>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Background" Value="#1AFFFFFF"/>
                                    <Setter Property="Foreground" Value="#38BDF8"/>
                                    <Setter Property="Padding" Value="10"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                </Style>
                                <Style TargetType="DataGridCell">
                                    <Setter Property="Padding" Value="10"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                </Style>
                            </DataGrid.Resources>
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="学籍番号 (ID)" Binding="{Binding StudentID}" Width="*"/>
                                <DataGridTextColumn Header="状態 (Status)" Binding="{Binding Status}" Width="*"/>
                                <DataGridTextColumn Header="備考 (Note)" Binding="{Binding Note}" Width="1.5*"/>
                            </DataGrid.Columns>
                        </DataGrid>

                        <StackPanel Grid.Row="2" Margin="0,20,0,0">
                            <TextBlock x:Name="lblNote" Text="修正理由（備考）：" Foreground="#94A3B8" FontWeight="SemiBold"/>
                            <Grid Margin="0,8,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="txtReason" Style="{StaticResource TechInputStyle}" Height="40"/>
                                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="15,0,0,0">
                                    <Button x:Name="btnPresent" Content="出席にする" Background="#10B981" Foreground="White" Width="120" Height="40" Click="btnMarkPresent_Click" Margin="0,0,10,0"/>
                                    <Button x:Name="btnAbsent" Content="欠席にする" Background="#EF4444" Foreground="White" Width="120" Height="40" Click="btnMarkAbsent_Click"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Grid>
                </Border>
            </TabItem>
        </TabControl>
    </Grid>
</Page>