<Page x:Class="Student_Attendance_System.Views.TeacherDashboard"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Teacher Dashboard" Background="{StaticResource WindowBackgroundBrush}"
      Loaded="Page_Loaded">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="TEACHER PORTAL" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextBrush}"/>
            <TextBlock x:Name="txtCurrentDate" Text="Loading Date..." Foreground="Gray"/>
        </StackPanel>

        <TabControl Grid.Row="1" x:Name="MainTabControl" Background="Transparent" BorderThickness="0">

            <TabItem Header=" 📅 Daily Schedule " FontSize="16" Height="40">
                <Grid Background="White" Margin="0,10,0,0">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="20">
                        <StackPanel>
                            <TextBlock Text="TODAY'S CLASSES (1C)" FontWeight="Bold" FontSize="18" Margin="0,0,0,15" Foreground="#2980b9"/>

                            <Border Background="#F9F9F9" BorderBrush="#DDD" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,0,10">
                                <Grid>
                                    <StackPanel>
                                        <TextBlock Text="PERIOD 1 (09:10 - 10:40)" FontWeight="Bold" Foreground="#2C3E50"/>
                                        <TextBlock x:Name="txtSubjP1" Text="Loading..." FontSize="16"/>
                                    </StackPanel>
                                    <Button x:Name="btnP1" Content="START CLASS" HorizontalAlignment="Right" Width="120" Height="35" Background="#27ae60" Foreground="White" Click="btnStart_Click"/>
                                </Grid>
                            </Border>

                            <Border Background="#F9F9F9" BorderBrush="#DDD" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,0,10">
                                <Grid>
                                    <StackPanel>
                                        <TextBlock Text="PERIOD 2 (10:50 - 12:20)" FontWeight="Bold" Foreground="#2C3E50"/>
                                        <TextBlock x:Name="txtSubjP2" Text="Loading..." FontSize="16"/>
                                    </StackPanel>
                                    <Button x:Name="btnP2" Content="START CLASS" HorizontalAlignment="Right" Width="120" Height="35" Background="Gray" Foreground="White" Click="btnStart_Click"/>
                                </Grid>
                            </Border>

                            <Border Background="#ECF0F1" Padding="10" Margin="0,0,0,10" CornerRadius="5">
                                <TextBlock Text="🍱 LUNCH BREAK (12:20 - 13:10)" HorizontalAlignment="Center" Foreground="Gray" FontWeight="SemiBold"/>
                            </Border>

                            <Border Background="#F9F9F9" BorderBrush="#DDD" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,0,10">
                                <Grid>
                                    <StackPanel>
                                        <TextBlock Text="PERIOD 3 (13:10 - 14:40)" FontWeight="Bold" Foreground="#2C3E50"/>
                                        <TextBlock x:Name="txtSubjP3" Text="Loading..." FontSize="16"/>
                                    </StackPanel>
                                    <Button x:Name="btnP3" Content="START CLASS" HorizontalAlignment="Right" Width="120" Height="35" Background="Gray" Foreground="White" Click="btnStart_Click"/>
                                </Grid>
                            </Border>

                            <Border Background="#F9F9F9" BorderBrush="#DDD" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,0,10">
                                <Grid>
                                    <StackPanel>
                                        <TextBlock Text="PERIOD 4 (14:50 - 16:20)" FontWeight="Bold" Foreground="#2C3E50"/>
                                        <TextBlock x:Name="txtSubjP4" Text="Loading..." FontSize="16"/>
                                    </StackPanel>
                                    <Button x:Name="btnP4" Content="START CLASS" HorizontalAlignment="Right" Width="120" Height="35" Background="Gray" Foreground="White" Click="btnStart_Click"/>
                                </Grid>
                            </Border>

                            <Border Background="#F9F9F9" BorderBrush="#DDD" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,0,10">
                                <Grid>
                                    <StackPanel>
                                        <TextBlock Text="PERIOD 5 (16:30 - 18:00)" FontWeight="Bold" Foreground="#2C3E50"/>
                                        <TextBlock x:Name="txtSubjP5" Text="Loading..." FontSize="16"/>
                                    </StackPanel>
                                    <Button x:Name="btnP5" Content="START CLASS" HorizontalAlignment="Right" Width="120" Height="35" Background="Gray" Foreground="White" Click="btnStart_Click"/>
                                </Grid>
                            </Border>

                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </TabItem>

            <TabItem Header=" 📝 Attendance Manager " FontSize="16" Height="40">
                <Border Background="White" Margin="0,10,0,0" Padding="20">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Margin="0,0,0,10">
                            <TextBlock Text="LIVE ATTENDANCE LIST" FontWeight="Bold" FontSize="18" Foreground="#2980b9"/>
                            <TextBlock Text="Select a student to modify their status (Reason Required)." Foreground="Gray"/>
                            <Button Content="🔄 Refresh List" HorizontalAlignment="Right" Click="btnRefresh_Click" Margin="0,-25,0,0"/>
                        </StackPanel>

                        <DataGrid Grid.Row="1" x:Name="dgAttendance" AutoGenerateColumns="False" 
                                  CanUserAddRows="False" IsReadOnly="True" 
                                  HeadersVisibility="Column" BorderBrush="#DDD" BorderThickness="1">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Student ID" Binding="{Binding StudentID}" Width="*"/>
                                <DataGridTextColumn Header="Time" Binding="{Binding Date}" Width="*"/>
                                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="0.8*"/>
                                <DataGridTextColumn Header="Note / Reason" Binding="{Binding Note}" Width="1.5*"/>
                            </DataGrid.Columns>
                        </DataGrid>

                        <Border Grid.Row="2" Background="#F9F9F9" CornerRadius="8" Padding="15" Margin="0,15,0,0" BorderBrush="#DDD" BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="Edit Reason (Required):" FontWeight="SemiBold" Margin="0,0,0,5"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBox x:Name="txtReason" Height="40" VerticalContentAlignment="Center" Padding="5" FontSize="14" BorderBrush="Gray" BorderThickness="1"/>

                                    <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="15,0,0,0">
                                        <Button Content="Mark PRESENT" Background="#27ae60" Foreground="White" Width="120" Click="btnMarkPresent_Click" Margin="0,0,10,0"/>
                                        <Button Content="Mark ABSENT" Background="#c0392b" Foreground="White" Width="120" Click="btnMarkAbsent_Click"/>
                                    </StackPanel>
                                </Grid>
                                <TextBlock Text="* Valid reason required (e.g., 'Train Delay', 'Sick')." Foreground="Red" FontSize="11" Margin="0,5,0,0"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </Border>
            </TabItem>

        </TabControl>
    </Grid>
</Page>