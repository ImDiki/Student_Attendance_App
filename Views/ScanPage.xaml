<Page x:Class="Student_Attendance_System.Views.ScanPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Scan / Webcam" Background="#F4F6F9">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Scan / Webcam" FontSize="28" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,15"/>

        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10">
            <Button Content="QR Code Scan" Width="200" Height="45" Background="#3498DB" Foreground="White" BorderThickness="0" Margin="5"/>
            <Button Content="Face Recognition" Width="200" Height="45" Background="White" Foreground="#2C3E50" BorderThickness="1" BorderBrush="#DDD" Margin="5"/>
        </StackPanel>

        <Border Grid.Row="1" Background="#1B2631" CornerRadius="10" Margin="0,10">
            <Grid>
                <Image x:Name="CameraPreview" Stretch="Uniform"/>
                <Border Width="250" Height="250" BorderBrush="#3498DB" BorderThickness="3" CornerRadius="15" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                <TextBlock Text="Position QR code within the frame" Foreground="White" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,0,0,20" Opacity="0.7"/>
            </Grid>
        </Border>

        <UniformGrid Grid.Row="2" Columns="4" Margin="0,15">
            <Border Background="White" CornerRadius="10" Margin="5" Padding="15">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="0" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <TextBlock Text="Checked In Today" Foreground="Gray" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
            <Border Background="White" CornerRadius="10" Margin="5" Padding="15">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="0" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <TextBlock Text="Total Students" Foreground="Gray" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
            <Border Background="White" CornerRadius="10" Margin="5" Padding="15">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="0" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Foreground="#F1C40F"/>
                    <TextBlock Text="Late" Foreground="Gray" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
            <Border Background="White" CornerRadius="10" Margin="5" Padding="15">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="0" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Foreground="#E74C3C"/>
                    <TextBlock Text="Absent" Foreground="Gray" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <Button Grid.Row="3" Content="Start Scanning" Width="250" Height="45" Background="#3498DB" Foreground="White" BorderThickness="0"  HorizontalAlignment="Center" Click="btnStart_Click"/>
    </Grid>
</Page>