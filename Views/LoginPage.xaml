<Page x:Class="Student_Attendance_System.Views.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Login" Background="{DynamicResource AppBackground}">

    <Grid>
        <Border Background="{DynamicResource SurfaceColor}" CornerRadius="20" Width="400" Padding="40" BorderBrush="{DynamicResource NeonBlue}" BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect Color="#00FFFF" BlurRadius="20" Opacity="0.2"/>
            </Border.Effect>

            <StackPanel>
                <TextBlock Text="ログイン" FontSize="28" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,40"/>

                <TextBlock Text="ユーザー名 (Username)" Foreground="{DynamicResource SecondaryText}" Margin="0,0,0,10"/>
                <TextBox x:Name="txtUser" Height="45" Margin="0,0,0,20"/>

                <TextBlock Text="パスワード (Password)" Foreground="{DynamicResource SecondaryText}" Margin="0,0,0,10"/>
                <PasswordBox x:Name="txtPass" Height="45" Margin="0,0,0,30" Background="#222" Foreground="White" BorderBrush="#444" Padding="10"/>

                <Button Content="ログイン (LOGIN)" Click="btnLogin_Click" Height="50" Margin="0,0,0,15"/>

                <Button Content="戻る (BACK)" Click="btnBack_Click" BorderThickness="0" Foreground="{DynamicResource SecondaryText}"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>