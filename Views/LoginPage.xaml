<Page x:Class="Student_Attendance_System.Views.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Login" Background="{StaticResource WindowBackgroundBrush}">

    <Grid>
        <Border Width="350" Height="480" Background="White" CornerRadius="15" Padding="30">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" Opacity="0.1"/>
            </Border.Effect>
            <StackPanel VerticalAlignment="Center">
                <TextBlock x:Name="txtTitle" Text="WELCOME" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center"/>
                <TextBlock x:Name="txtSubTitle" Text="Sign in to continue" FontSize="14" HorizontalAlignment="Center" Margin="0,5,0,30" Foreground="Gray"/>

                <TextBlock x:Name="lblUsername" Text="Username" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <TextBox x:Name="txtUsername" Height="35" VerticalContentAlignment="Center" Padding="5,0"/>

                <TextBlock x:Name="lblPassword" Text="Password" FontWeight="SemiBold" Margin="0,15,0,5"/>
                <PasswordBox x:Name="txtPassword" Height="35" VerticalContentAlignment="Center" Padding="5,0"/>

                <Button x:Name="btnLogin" Content="LOGIN" Height="45" Margin="0,30,0,0" Background="#2980b9" Foreground="White" FontWeight="Bold" Click="btnLogin_Click"/>

                <TextBlock x:Name="txtForgetPass" Text="Forgot Password?" Foreground="Gray" Cursor="Hand" HorizontalAlignment="Center" Margin="0,15,0,0" MouseLeftButtonDown="ForgetPassword_Click"/>
                <TextBlock x:Name="txtRegisterLink" Text="No account? Register here." Foreground="#2980b9" Cursor="Hand" HorizontalAlignment="Center" Margin="0,10,0,0" MouseLeftButtonDown="GoToRegister_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>