<Page x:Class="Student_Attendance_System.Views.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Login" Background="#F0F2F5">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" Padding="15">
            <StackPanel Orientation="Horizontal">
                <Button Content="⬅ 戻る (Back)" Width="100" Height="35" Background="White" Foreground="#333" 
                        FontWeight="Bold" BorderThickness="1" Click="btnBack_Click"/>
                <TextBlock Text="管理者ログイン" FontSize="20" FontWeight="Bold" VerticalAlignment="Center" Margin="20,0,0,0"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Background="White" Width="400" Height="350" CornerRadius="15" Padding="30" VerticalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect Color="#DDD" BlurRadius="10" ShadowDepth="3"/>
            </Border.Effect>

            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="管理者専用 (Admin Only)" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,30" Foreground="#333"/>

                <TextBlock Text="ユーザー名 (Username):" FontWeight="Bold" Foreground="#555"/>
                <TextBox x:Name="txtUsername" Height="35" Margin="0,5,0,15" Padding="5" FontSize="14"/>

                <TextBlock Text="パスワード (Password):" FontWeight="Bold" Foreground="#555"/>
                <PasswordBox x:Name="txtPassword" Height="35" Margin="0,5,0,30" Padding="5" FontSize="14"/>

                <Button Content="ログイン (Login)" Height="45" Background="#4CAF50" Foreground="White" 
                        FontSize="16" FontWeight="Bold" Click="btnLogin_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</Page>